# CPU-only requirements for systems without CUDA/GPU support
# This file ensures compatibility with diffusers and avoids CUDA dependencies

# PyTorch CPU-only (explicitly specify CPU versions)
--index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Core AI libraries
diffusers>=0.21.0
transformers>=4.30.0
accelerate>=0.20.0

# Image and video processing
Pillow>=10.0.0
opencv-python>=4.8.0
numpy>=1.24.0
scipy>=1.10.0

# Utilities
requests>=2.31.0
tqdm>=4.65.0
safetensors>=0.3.0

# Web interface
flask>=2.3.0
python-dotenv>=1.0.0
gunicorn>=21.0.0

# Note: xformers is explicitly excluded as it requires CUDA and compiled C++ extensions
# Note: Uses CPU-specific PyTorch index to avoid CUDA dependencies